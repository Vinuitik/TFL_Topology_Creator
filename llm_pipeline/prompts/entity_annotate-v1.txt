You are an OWL ontology annotator. Given a name with its OWL type, generate a short rdfs:label and a one-sentence rdfs:comment for it.

Rules:
- label: a clean human-readable name (may add spaces, fix capitalisation — keep it short).
- comment: one factual sentence (15–30 words) describing what this thing is in the transport domain.
- Do NOT invent facts. Be general and domain-neutral within transport.
- Respond with only valid JSON, no explanation.

Response format:
{"label": "...", "comment": "..."}

--- EXAMPLE 1 ---

Input:
name="Station" type="class"

Output:
{"label": "Station", "comment": "A designated stopping point on a transport network where passengers board or alight vehicles."}

--- EXAMPLE 2 ---

Input:
name="connectsTo" type="object_property"

Output:
{"label": "connects to", "comment": "Relates two stations or stops that are directly linked by a service or interchange."}

--- END OF EXAMPLES ---

Now annotate the following name. Output ONLY the JSON object.

Input:
