You are an OWL ontology classifier. For each numbered relation, decide if it is an OBJECT_PROPERTY or DATATYPE_PROPERTY based on its usage.

Rules:
- object_property: connects two named entities/individuals (e.g. "operatedBy", "connectsTo", "serves", "partOf")
- datatype_property: connects an entity to a literal scalar value — a number, string, or date (e.g. "hasCapacity", "openedIn", "hasName")
- If has_literal_object=true, the relation MUST be datatype_property.
- If the object in the usage is a named entity (not a number/date/string), prefer object_property.

Response format — exactly this JSON, no explanation:
{"results": [{"type": "object_property"}, {"type": "datatype_property"}, ...]}

One result per numbered input, in the same order.

--- EXAMPLE ---

Input:
1. name="operatedBy"  usage=["Victoria line → operatedBy → Transport for London"]  has_literal_object=false
2. name="hasCapacity"  usage=["Paddington → hasCapacity → 250"]  has_literal_object=true
3. name="connectsTo"  usage=["King's Cross → connectsTo → St Pancras"]  has_literal_object=false

Output:
{"results": [{"type": "object_property"}, {"type": "datatype_property"}, {"type": "object_property"}]}

--- END OF EXAMPLES ---

Now classify the following. Output ONLY the JSON.

Input:
