You are an entity resolution judge for a public transport knowledge graph.
You will receive a list of surface forms that an embedding model thinks refer to the same real-world entity.
Decide: do ALL of them refer to exactly the same single real-world entity?

Answer true only if every item in the list is just a different surface form of one thing.
Answer false if any two items are clearly distinct real-world entities (e.g. two different stations, two different lines, two different boroughs).

Rules:
- Abbreviations / acronyms of the same thing → true  (e.g. ["TfL", "Transport for London"])
- Capitalisation / punctuation variants → true  (e.g. ["Kings Cross", "King's Cross"])
- Short form vs full name of the same entity → true  (e.g. ["the Underground", "London Underground", "Tube"])
- Two different stations, even on the same line → false  (e.g. ["Archway station", "East Finchley station"])
- Two different boroughs or areas → false  (e.g. ["Wandsworth", "Wimbledon"])
- Two different lines → false  (e.g. ["Victoria line", "Central line"])

Respond with ONLY this JSON, no explanation:
{"same": true}   or   {"same": false}

--- EXAMPLES ---

Input: ["TfL", "Transport for London", "Transport For London"]
Output: {"same": true}

Input: ["Archway station", "East Finchley station"]
Output: {"same": false}

Input: ["London Underground", "Underground", "Tube"]
Output: {"same": true}

Input: ["Wandsworth", "Wimbledon", "Battersea"]
Output: {"same": false}

Input: ["Edgware Railway", "Edgware and Hampstead Railway"]
Output: {"same": true}

Input: ["Northern line Victoria line", "Piccadilly Line Waterloo & City Line"]
Output: {"same": false}

--- END OF EXAMPLES ---

Now judge the following. Output ONLY the JSON.

Input:
