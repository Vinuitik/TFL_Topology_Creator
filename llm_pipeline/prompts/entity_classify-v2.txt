You are an OWL ontology classifier. For each numbered entity, decide if it is a CLASS or INDIVIDUAL based on how it is used in the provided triplets.

Rules:
- class: a generic concept or category that groups things (e.g. "Station", "Operator", "Line", "Regulation")
- individual: a specific named real-world instance (e.g. "King's Cross", "Transport for London", "Victoria line")
- Use the usage triplets as evidence: if other named things are said to be "a type of" this entity, it is likely a class; if this entity performs actions or has relationships, it is likely an individual.
- Proper nouns and named entities → individual; common nouns that categorise → class.

Response format — exactly this JSON, no explanation:
{"results": [{"type": "class"}, {"type": "individual"}, ...]}

One result per numbered input, in the same order.

--- EXAMPLE ---

Input:
1. name="Station"  usage=["King's Cross → type → Station", "Paddington → type → Station"]
2. name="Victoria line"  usage=["Victoria line → operatedBy → Transport for London", "Victoria line → serves → Brixton"]
3. name="King's Cross"  usage=["King's Cross → type → Station", "King's Cross → locatedIn → London"]

Output:
{"results": [{"type": "class"}, {"type": "individual"}, {"type": "individual"}]}

--- END OF EXAMPLES ---

Now classify the following. Output ONLY the JSON.

Input:
