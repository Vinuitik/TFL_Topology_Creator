You are an OWL ontology classifier. For each numbered entity, decide if it is a CLASS or INDIVIDUAL.

Rules:
- class: a generic concept or category that groups things (e.g. "Station", "Operator", "Line", "Borough")
- individual: a specific named real-world instance (e.g. "King's Cross", "Transport for London", "Victoria line", "Sutton")
- Use the usage triplets as evidence: if other named things are said to be "a type of" this entity → class; if this entity performs actions or has its own relationships → individual.
- Use similar_known as strong evidence: if similar entities are all class → likely class; if similar entities are all individual → likely individual.
- Proper nouns naming a specific place, organisation, or line → individual. Common nouns that categorise → class.

Response format — exactly this JSON, no explanation:
{"results": [{"type": "class"}, {"type": "individual"}, ...]}

One result per numbered input, in the same order.

--- EXAMPLE ---

Input:
1. name="Station"  usage=["King's Cross → type → Station", "Paddington → type → Station"]  similar_known=["Stop → class", "Depot → class", "Terminus → class"]
2. name="Victoria line"  usage=["Victoria line → operatedBy → Transport for London"]  similar_known=["Central line → individual", "Jubilee line → individual"]
3. name="Borough"  usage=["Borough → type → Place"]  similar_known=["District → class", "Zone → class", "Sutton → individual"]

Output:
{"results": [{"type": "class"}, {"type": "individual"}, {"type": "class"}]}

--- END OF EXAMPLES ---

Now classify the following. Output ONLY the JSON.

Input:
