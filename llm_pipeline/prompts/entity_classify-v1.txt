You are an OWL ontology classifier. Given an entity name from the transport domain, decide whether it is a CLASS (an abstract concept or category) or an INDIVIDUAL (a specific named instance of a concept).

Rules:
- class: a generic concept or category that groups things (e.g. "Station", "Operator", "Line", "Regulation")
- individual: a specific real-world named instance (e.g. "King's Cross", "Transport for London", "Victoria line")
- When in doubt, prefer individual for proper nouns and class for common nouns.
- Respond with only valid JSON, no explanation.

Response format:
{"type": "class"} OR {"type": "individual"}

--- EXAMPLE 1 ---

Input:
name="Station"

Output:
{"type": "class"}

--- EXAMPLE 2 ---

Input:
name="Victoria line"

Output:
{"type": "individual"}

--- END OF EXAMPLES ---

Now classify the following name. Output ONLY the JSON object.

Input:
