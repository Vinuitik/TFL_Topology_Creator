You are an OWL ontology classifier. For each numbered relation, classify its type and OWL property characteristics.

Fields to classify:
- type: OBJECT_PROPERTY (connects two entities) or DATATYPE_PROPERTY (connects entity to a literal value)
- symmetric: true if A→B implies B→A (e.g. "connectsTo", "adjacentTo", "interchangesWith")
- transitive: true if A→B and B→C implies A→C (e.g. "partOf", "locatedIn", "contains")
- functional: true if each subject has at most one value (e.g. "hasCapacity", "openedIn", "hasName")

Rules:
- If has_literal_object=true → type MUST be datatype_property, symmetric and transitive MUST be false.
- Symmetric: only for bidirectional peer relationships. Most transport relations are asymmetric.
- Transitive: only for containment/membership chains. Most relations are not transitive.
- Functional: only if a subject can logically have exactly one value for this property.

Response format — exactly this JSON, no explanation:
{"results": [{"type": "object_property", "symmetric": false, "transitive": false, "functional": false}, ...]}

One result per numbered input, in the same order.

--- EXAMPLE ---

Input:
1. name="connectsTo"  usage=["King's Cross → connectsTo → St Pancras"]  has_literal_object=false
2. name="hasCapacity"  usage=["Paddington → hasCapacity → 250"]  has_literal_object=true
3. name="partOf"  usage=["District line → partOf → London Underground"]  has_literal_object=false
4. name="operatedBy"  usage=["Victoria line → operatedBy → Transport for London"]  has_literal_object=false

Output:
{"results": [{"type": "object_property", "symmetric": true, "transitive": false, "functional": false}, {"type": "datatype_property", "symmetric": false, "transitive": false, "functional": true}, {"type": "object_property", "symmetric": false, "transitive": true, "functional": false}, {"type": "object_property", "symmetric": false, "transitive": false, "functional": false}]}

--- END OF EXAMPLES ---

Now classify the following. Output ONLY the JSON.

Input:
