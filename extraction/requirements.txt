# Core ontology
rdflib>=6.3.0

# Schema generation
schema-automator>=0.2.0

# Knowledge graph extraction
ontogpt>=0.4.0

# HTTP requests (Ollama)
requests>=2.31.0

# YAML processing
pyyaml>=6.0.1

# LinkML runtime (installed as ontogpt dependency but pinning for safety)
linkml>=1.6.0
linkml-runtime>=1.6.0